export interface Post {
  title: string;
  content: string;
  reference: string;
  DOB: string;
  Doctor: String;
  Diagnosis: String;
}
/*-----Here I declared my variables and stated their type which are all strings ----- */
/*-----The client will enter values for these from the form page which will be passed to the server----- */